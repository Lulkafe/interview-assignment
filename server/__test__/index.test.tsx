import server from "../src";

afterAll(done => {
    server.close();
    done();
})


describe("API calls", () => {
    it("", () => {
        
    })
});


describe("Server", () => {
    it("", () => {

    })
})
