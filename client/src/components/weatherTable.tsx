import React from "react";
import { ResultTableProps, SearchResult } from "../types";


const WeatherTable = (props: ResultTableProps) => {

    const { results }: { results: SearchResult[] } = props;

    return (
        <table>
            
        </table>
    )
}